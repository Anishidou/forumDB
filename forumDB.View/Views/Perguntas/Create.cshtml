@model UsuPer;
@using forumDB.Model;
@using forumDB.View.Models


@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Usuario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="oPergunta.Titulo" class="control-label"></label>
                <input asp-for="oPergunta.Titulo" class="form-control" />
                <span asp-validation-for="oPergunta.Titulo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="oPergunta.Texto" class="control-label"></label>
                <input asp-for="oPergunta.Texto" class="form-control" />
                <span asp-validation-for="oPergunta.Texto" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="oPergunta.IdUsuario" class="control-label"></label>
                <input asp-for="oPergunta.IdUsuario" class="form-control" />
                <span asp-validation-for="oPergunta.IdUsuario" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="oPergunta.NomeCurso" class="control-label"></label>
                <select class="select" style="width: 100%; height: 35px" asp-for="oPergunta.NomeCurso" asp-items="ViewBag.NomeCurso"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
